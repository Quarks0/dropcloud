import { values } from 'lodash';

export const selectAllSongs = (songs) => values(songs);

export const selectSongDetail = (songDetail) => values(songDetail);

export const selectAllUsers = (users) => values(users);

export const selectUserDetail = (userDetail) => values(userDetail);
